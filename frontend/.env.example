# CodeMentor AI — Frontend Environment Configuration Example
# Copy this file to .env.local and adjust values if needed.
# NEVER commit .env.local to version control.

# API base URL — leave empty to use the Vite proxy during development.
# Only set this if you are NOT using the proxy (e.g., pointing to a remote backend).
VITE_API_BASE_URL=
